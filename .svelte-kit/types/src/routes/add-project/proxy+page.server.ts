// @ts-nocheck
import type { PageServerLoad } from './$types';

export const load = async () => {
    return {
        // Add any server-side data needed for the page here
    };
};
;null as any as PageServerLoad;